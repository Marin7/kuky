package com.kuky.backend.placement.model;

/** Placement-local mirror of learning.QuestionKind — kept decoupled on purpose (see research.md §1). */
public enum QuestionKind {
    SINGLE_CHOICE, MULTI_CHOICE, FILL_BLANK
}
