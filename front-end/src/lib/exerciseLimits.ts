/** Caps for structured exercise blanks / word banks (specs/034-multi-correct-blanks). */
export const MAX_ACCEPTED_PER_BLANK = 10;
export const MAX_BANK_ITEMS = 30;

/** Max numbered `(1)`…`(N)` items on one opción única (specs/038-single-choice-multi). */
export const MAX_SINGLE_CHOICE_ITEMS = 20;
